# Repo-local tools

This repository keeps security tooling under `tools/bin` so commands are reproducible across environments.

## Install

```bash
scripts/install-tools.sh
```

## One-time Gitleaks history scan

```bash
scripts/gitleaks-history-scan.sh
```

Default report path:

- `tools/gitleaks-history-report.json`
